export const ADDON_ID = 'storybook-addon-koval';
export const TOOL_ID = `${ADDON_ID}/tool`;
export const PANEL_ID = `${ADDON_ID}/panel`;

export const EVENTS = {
    SET_THEME: `${ADDON_ID}/set_theme`,
    RESET_THEME: `${ADDON_ID}/reset_theme`,
};
