import { Meta, ArgTypes, Story, Canvas, Source, Primary } from "@storybook/blocks";
import * as InputTextStories from "./InputText.stories";
import { InputText } from "./InputText";

<Meta title="Inputs/Text" of={InputTextStories} />

# Text Input

`InputText` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type.  It supports various text-like input types (for example, `text`, `email`, `password`).

## Imports

<Source format={true} language="ts" code={`
import {InputText} from 'koval-ui';
`} />

<Primary />

## Icon props

<ArgTypes of={InputText} />



