import { Meta, ArgTypes, Story, Canvas, Markdown, Primary } from "@storybook/blocks";
import * as FormFieldStories from "./FormField.stories";
import { FormField } from "./FormField";

<Meta title="Inputs/FormField" of={FormFieldStories} />

# Form Field

`FormField` component is a customizable and accessible form field container. It supports labels, hint text, and required indicators, and ensures proper association between labels and input elements. The component integrates seamlessly with various input elements.

## Imports

<Markdown>{`
\`\`\`ts
import {FormField} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

## Icon props

<ArgTypes of={FormField} />




