import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import * as FormStories from "./Form.stories.tsx";
import { Form, FormVanilla } from "./Form.tsx";

<Meta title="Inputs/Form" of={FormStories} />

# Form

`Form` is a customizable and accessible input built as a drop-in replacement for `HTMLFormElement`.

## Imports

<Markdown>{`
\`\`\`ts
import {Form} from 'koval-ui';
\`\`\`
`}</Markdown>

## Form props

<ArgTypes of={FormVanilla} />


<Primary />


