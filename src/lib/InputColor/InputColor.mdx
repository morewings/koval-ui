import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {InputColor} from './InputColor.tsx';
import * as InputColorStories from "./InputColor.stories.tsx";

<Meta title="Inputs/Color" of={InputColorStories} />

# Color Input

`InputColor` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type. It allows users to select colors and supports predefined color lists.

## Imports

<Markdown>{`
\`\`\`ts
import {InputColor} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={InputColor}/>

