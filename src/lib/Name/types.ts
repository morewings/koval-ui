// Works for currency and time, doesn't work for region and language. TODO: verify again.
export enum Styles {
    narrow = 'narrow',
    short = 'short',
    long = 'long',
}

export enum TimePeriods {
    era = 'era',
    year = 'year',
    quarter = 'quarter',
    month = 'month',
    weekOfYear = 'weekOfYear',
    weekday = 'weekday',
    day = 'day',
    dayPeriod = 'dayPeriod',
    hour = 'hour',
    minute = 'minute',
    second = 'second',
    timeZoneName = 'timeZoneName',
}
