import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {Button} from './Button.tsx';
import * as ButtonStories from "./Button.stories";

<Meta title="Inputs/Button" component={Button} of={ButtonStories} />

# Button

`Button` renders `HTMLButtonElement` with Koval UI styling.

## Imports

<Markdown>{`
\`\`\`ts
import {Button} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={Button}/>

