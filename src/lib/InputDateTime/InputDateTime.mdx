import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {InputDateTime} from './InputDateTime.tsx';
import * as InputDateTimeStories from "./InputDateTime.stories.tsx";

<Meta title="Inputs/DateTime" of={InputDateTimeStories} />

# InputDateTime

`InputDateTime` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type. It combines `InputDate` and `InputTime` components, allowing users to select both date and time.

## Imports

<Markdown>{`
\`\`\`ts
import {InputDateTime} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={InputDateTime}/>

