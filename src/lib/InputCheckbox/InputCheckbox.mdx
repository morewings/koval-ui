import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import * as InputCheckboxStories from "./InputCheckbox.stories";
import { InputCheckbox } from "./InputCheckbox.tsx";

<Meta title="Inputs/Checkbox" of={InputCheckboxStories} />

# Checkbox

## Description

`InputCheckbox` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type. It supports various states and includes validation features. The component provides options for custom labels.

## Imports

<Markdown>{`
\`\`\`ts
import {InputCheckbox} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

## Checkbox props

<ArgTypes of={InputCheckbox} />



