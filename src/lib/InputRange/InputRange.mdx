import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {InputRange} from './InputRange.tsx';
import * as InputRangeStories from "./InputRange.stories.tsx";

<Meta title="Inputs/Range" of={InputRangeStories} />

# InputRange

`InputRange` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type. The component allows users to select a value within a specified range using a slider and provides options for displaying scale marks.

## Imports

<Markdown>{`
\`\`\`ts
import {InputRange} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={InputRange}/>

