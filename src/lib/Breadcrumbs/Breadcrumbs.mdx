import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {Breadcrumbs} from './Breadcrumbs.tsx';
import * as BreadcrumbsStories from "./Breadcrumbs.stories.tsx";

<Meta title="Components/Breadcrumbs" of={BreadcrumbsStories} />

# Breadcrumbs

## Description

`Breadcrumbs` component is a navigation element. It supports properties for rendering a list of breadcrumb items, optionally showing an ellipsis after the first item, and using a custom link component

## Imports

<Markdown>{`
\`\`\`ts
import {Breadcrumbs} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={Breadcrumbs}/>

