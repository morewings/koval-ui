import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {InputTime} from './InputTime.tsx';
import * as InputTimeStories from "./InputTime.stories.tsx";

<Meta title="Inputs/Time" of={InputTimeStories} />

# InputTime

`InputTime` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type. It allows a user to enter time.

## Imports

<Markdown>{`
\`\`\`ts
import {InputTime} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={InputTime}/>

