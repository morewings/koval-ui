import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {BlockQuote} from './BlockQuote.tsx';
import * as BlockQuoteStories from "./BlockQuote.stories.tsx";

<Meta title="Typography/BlockQuote" of={BlockQuoteStories} />

# BlockQuote

`BlockQuote` provides a styled wrapper for quotes including `<cite>` tag.

## Imports

<Markdown>{`
\`\`\`ts
import {BlockQuote} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={BlockQuote}/>

