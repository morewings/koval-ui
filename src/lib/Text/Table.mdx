import { Meta, ArgTypes, Stories, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {Table} from './Table.tsx';
import * as TableStories from "./Table.stories.tsx";

<Meta title="Typography/Header" of={TableStories} />

# Table

`Table` component is designed to display tabular data in a structured and accessible manner.

## Imports

<Markdown>{`
\`\`\`ts
import {Table} from 'koval-ui';
\`\`\`
`}</Markdown>

<ArgTypes of={Table}/>

<Primary />

<Stories includePrimary={false} />

