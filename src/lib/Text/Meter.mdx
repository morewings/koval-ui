import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {Meter} from './Meter.tsx';
import * as MeterStories from "./Meter.stories.tsx";

<Meta title="Typography/Meter" of={MeterStories} />

# Meter

## Description

`Meter` is component to show progress.  It supports properties such as `max`, `min`, `value`, `low`, `high`, and `optimum`.

## Imports

<Markdown>{`
\`\`\`ts
import {Meter} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={Meter}/>

