import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {InputPassword} from './InputPassword.tsx';
import * as InputPasswordStories from "./InputPassword.stories.tsx";

<Meta title="Inputs/Password" of={InputPasswordStories} />

# InputPassword

`InputPassword` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type.  It supports toggling between password (`***`) and plain text visibility.

## Imports

<Markdown>{`
\`\`\`ts
import {InputPassword} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={InputPassword}/>

