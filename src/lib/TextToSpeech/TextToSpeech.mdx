import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import { TextToSpeech } from "./TextToSpeech.tsx";
import * as TextToSpeechStories from "./TextToSpeech.stories.tsx";

<Meta title="Components/TextToSpeech" of={TextToSpeechStories} />

# TextToSpeech

## Description

`TextToSpeech` implements `SpeechSynthesis API` allowing to read the text using system synthesizer.

## Imports

<Markdown>{`
\`\`\`ts
import {TextToSpeech} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={TextToSpeech} />
