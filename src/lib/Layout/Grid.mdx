import { Meta, ArgTypes, Story, Primary, Markdown } from "@storybook/blocks";
import * as GridStories from './Grid.stories';
import { Grid } from "./Grid.tsx";
import {Row} from './Row';
import {Col} from './Col';

<Meta title="Layout/Grid" of={GridStories} />

# Grid

## Description

`Grid` component in Koval UI is a customizable layout container. It allows a developer to define the container width, number of columns, and gap between columns, making it easy to create responsive grid layouts, and also supports fluid width.

## Imports

<Markdown>{`
\`\`\`ts
import {Grid} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

## Grid props

<ArgTypes of={Grid} />

## Row props

<ArgTypes of={Row} />

## Col props

<ArgTypes of={Col} />





