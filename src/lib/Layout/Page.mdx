import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary, Controls } from "@storybook/blocks";
import {Page} from './Page.tsx';
import {Header} from './Header.tsx';
import {Footer} from './Footer.tsx';
import {Main} from './Main.tsx';
import {Sidebar} from './Sidebar.tsx';
import {Content} from './Content.tsx';
import * as PageStories from "./Page.stories.tsx";

<Meta title="Layout/Page" of={PageStories} />

# Select

`Page` component in Koval UI is a layout container. It extends the `Grid` component, allowing a developer to use `Header`, `Content`, `Sidebar`, `Main`, and `Footer`.

## Imports

<Markdown>{`
\`\`\`ts
import {Page, Header, Content, Sidebar, Main, Footer} from 'koval-ui';
\`\`\`
`}</Markdown>

## Page props

<ArgTypes of={Page}/>

<Primary />

## Header props

<ArgTypes of={Header}/>

## Content props

<ArgTypes of={Content}/>

## Footer props

<ArgTypes of={Footer}/>

## Sidebar props

<ArgTypes of={Sidebar}/>

## Main props

<ArgTypes of={Main}/>

