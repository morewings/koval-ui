import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {Dialog} from './Dialog.tsx';
import * as DialogStories from "./Dialog.stories.tsx";

<Meta title="Components/Dialog" of={DialogStories} />

# Dialog

## Description

`Dialog` renders a modal over the main content flow. Allows global control of the various dialog windows.

## Imports

<Markdown>{`
\`\`\`ts
import {Dialog, DialogFooter, DialogBody, DialogHeader, useDialogState} from 'koval-ui';
\`\`\`
`}</Markdown>

<ArgTypes of={Dialog}/>

<Primary />

