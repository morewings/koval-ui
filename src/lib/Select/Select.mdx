import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {Select} from './Select.tsx';
import * as SelectStories from "./Select.stories.tsx";

<Meta title="Inputs/Select" of={SelectStories} />

# Select

There is no `defaultValue` value prop here due to browser implementation. Use `<option selected>value</option>` instead.

## Imports

<Markdown>{`
\`\`\`ts
import {Select} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={Select}/>

