import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {InputNumber} from './InputNumber.tsx';
import * as InputNumberStories from "./InputNumber.stories.tsx";

<Meta title="Inputs/Number" of={InputNumberStories} />

# InputNumber

`InputNumber` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type. It allows users to enter numbers. The component allows users to increment or decrement values using spinner controls

## Imports

<Markdown>{`
\`\`\`ts
import {InputNumber} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={InputNumber}/>

