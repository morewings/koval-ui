import { Meta, ArgTypes, Story, Canvas, Source, Markdown, Primary } from "@storybook/blocks";
import {InputFile} from './InputFile.tsx';
import * as InputFileStories from "./InputFile.stories.tsx";

<Meta title="Inputs/File" of={InputFileStories} />

# File Input

`InputFile` is a customizable and accessible input built as a drop-in replacement for `HTMLInputElement` of corresponding type. It allows users to select files for uploading.

## Imports

<Markdown>{`
\`\`\`ts
import {InputFile} from 'koval-ui';
\`\`\`
`}</Markdown>

<Primary />

<ArgTypes of={InputFile}/>

